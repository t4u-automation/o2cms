# O2 CMS Environment Configuration
# Copy this file to .env.local and fill in your values

# ===========================================
# Firebase Configuration (Required)
# ===========================================
# Get these values from Firebase Console > Project Settings > General > Your apps

NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# ===========================================
# API Configuration (Required)
# ===========================================
# URL to your deployed Cloud Functions API

NEXT_PUBLIC_API_BASE_URL=https://us-central1-your-project.cloudfunctions.net/api

# ===========================================
# Optional Configuration
# ===========================================

# GraphQL API URL (defaults to same project)
# NEXT_PUBLIC_GRAPHQL_URL=https://us-central1-your-project.cloudfunctions.net/graphql

# App Base URL (for invitation links)
# APP_BASE_URL=https://your-domain.com

# Migration function URL (required for Contentful migration feature)
# NEXT_PUBLIC_MIGRATION_FUNCTION_URL=https://runmigrationjob-HASH.a.run.app

# ===========================================
# Cloud Functions Secrets (set via GCP Secret Manager)
# ===========================================
# These are NOT set in .env.local — they are configured via
# Google Cloud Secret Manager. See DEPLOYMENT.md for setup instructions.
#
# SENDGRID_API_KEY          - SendGrid API key for invitation emails (optional)
# SENDGRID_FROM_EMAIL       - Sender email address (default: no-reply@o2cms.com)
# SENDGRID_FROM_NAME        - Sender display name (default: O2 CMS)
# APP_BASE_URL              - Base URL for invitation links (default: https://o2cms.com)
# TYPESENSE_HOST            - Typesense server hostname (optional)
# TYPESENSE_PORT            - Typesense server port (default: 443)
# TYPESENSE_PROTOCOL        - Typesense protocol (default: https)
# TYPESENSE_ADMIN_API_KEY   - Typesense admin API key (optional)

